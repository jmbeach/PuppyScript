treat myTreat = "This one's on me";
treat someMath = 1 + 1;
treat resultantMath = someMath / 2;
trick myTrick(a, b) {
	doTrick(1+1,2+2);
}
ruff(rusultantMath < 4) {
	puppyPrint("Wow we did it");
	treat i = 0;
	while(i < 10) {
		puppyPrint("My number is",i);
		ruff (i / 2 == 4) {
			trick rollover(dog) {
				yellat(dog);
			}(myDog);
		}
		relse ruff (isDogTired(dog)) {
			puppyPrint("be nice to your dog!!!");
		}
		relse {
			puppyPrint("Now is not the time for tricks");
		}
	}
}
